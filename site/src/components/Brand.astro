---
---

<div class="brand">
  <span class="brand-icon" aria-hidden="true">
    <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
      <!-- Core planet -->
      <circle cx="10" cy="10" r="6.5" stroke="currentColor" stroke-width="1.8" />
      <!-- Orbit (tilted for motion) -->
      <ellipse cx="10" cy="10" rx="8.5" ry="4.2" stroke="currentColor" stroke-width="1" opacity="0.62" transform="rotate(-20 10 10)" />
      <!-- Data nodes -->
      <circle cx="6.2" cy="7.4" r="0.9" fill="currentColor" />
      <circle cx="13.6" cy="10.6" r="0.9" fill="currentColor" />
      <circle cx="9.4" cy="13.8" r="0.7" fill="currentColor" opacity="0.85" />
      <!-- Tiny “signal” arc (subtle tech detail) -->
      <path d="M13.2 5.4c1.2.4 2.2 1.2 2.9 2.3" stroke="currentColor" stroke-width="1" opacity="0.35" stroke-linecap="round"/>
    </svg>
  </span>

  <span class="brand-wordmark" aria-label="Open Data Orbit">
    <span class="wm-accent">Open Data</span>
    <span class="wm-core">Orbit</span>
  </span>
</div>

<style>
.brand {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  font-family: var(--font-display, inherit);
  font-weight: 800;
  font-size: 1.18rem;
  letter-spacing: 0.02em;
}

.brand-icon {
  display: flex;
  align-items: center;
  height: 20px;
  width: 20px;
  margin-right: 2px;
}

.brand-icon svg {
  display: block;
  color: var(--accent3);
  filter: drop-shadow(0 0 10px rgba(88,166,255,0.25));
  transition: filter 0.18s, color 0.18s, opacity 0.18s;
}

.brand:hover .brand-icon svg {
  color: var(--accent2);
  filter: drop-shadow(0 0 16px rgba(88,166,255,0.45));
}

.brand-wordmark {
  display: inline-flex;
  align-items: baseline;
  gap: 0.45rem;
  line-height: 1;
}

/* Open Data (accent gradient, NO white) */
.brand-wordmark .wm-accent {
  background: linear-gradient(90deg, var(--accent1) 0%, var(--accent2) 50%, var(--accent3) 100%);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}

/* Orbit (clean, premium “almost white”) */
.brand-wordmark .wm-core {
  color: rgba(230, 237, 243, 0.94);
  text-shadow: 0 10px 40px rgba(31,111,235,0.10);
}

/* Optional hover: subtle brightening only (no full rainbow) */
.brand:hover .brand-wordmark .wm-core {
  color: rgba(255,255,255,0.96);
}
</style>
