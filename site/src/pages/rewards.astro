---
import Layout from '../layouts/Layout.astro';
import rewards from '../data/rewards.json';
---


<Layout title="Rewards" description="Badges och incitament för contributions">
  <h1>Badges & Rewards</h1>
  <p>
    I Open Data Hub SE kan du få badges för att bidra, hjälpa andra och vara aktiv i communityn.
  </p>

  <div class="rewards-info">
    <h2>Hur funkar rewards?</h2>
    <p>
      I MVP-versionen delas badges ut <strong>manuellt av moderatorer</strong> baserat på dina insatser. Läs mer om hur du kan bidra i <a href="/CONTRIBUTING.md" target="_blank">CONTRIBUTING.md</a>.
    </p>
  </div>

  <div class="badges-list">
    {rewards.map((badge) => (
      <div class="badge-card" key={badge.id}>
        <div class="badge-icon">🏅</div>
        <div class="badge-info">
          <h3>{badge.name}</h3>
          <p>{badge.description}</p>
          <p class="badge-criteria"><strong>Hur får man den?</strong> {badge.criteria}</p>
        </div>
      </div>
    ))}
  </div>
</Layout>

<style>
  .badges-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: var(--spacing-lg);
    margin-top: var(--spacing-xl);
  }

  .badge-card {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-lg);
    padding: var(--spacing-lg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background-color: var(--color-bg-secondary);
  }

  .badge-icon {
    font-size: 2.5rem;
    margin-right: var(--spacing-md);
  }

  .badge-info h3 {
    margin: 0 0 var(--spacing-xs) 0;
    font-size: var(--font-size-lg);
  }

  .badge-info p {
    margin: 0;
    color: var(--color-text-secondary);
  }
</style>
